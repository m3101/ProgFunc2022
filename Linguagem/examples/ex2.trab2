/*
 O programa abaixo eh valido (bem-tipado) e retorna o contexto abaixo ao ser executado:
([[(Ident "f",720),(Ident "return",720)]],[(Ident "main",int<-()),(Ident "fat",int<-(int n))])
*/


int main () {
  int f = fat (5+1);
  return f;
}

int fat (int n) {
 if (n)
   then return n * fat (n-1);
   else return 1;
}
